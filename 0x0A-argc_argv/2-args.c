#include<stdio.h>
/**
 *main - program name
 *@argc: argument size
 *@argv: argument value
 *Return: always 0 for success
 */
int main(int argc, *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		argv[i] = argc[i]
	}
	printf("argv[i]: %s\n", i, argv[i]);

	return (0);



}
